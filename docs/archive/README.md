# Archive — completed or superseded, kept for the record

Nothing in this folder is live. Each file is here because its work is **done** or its content is
**superseded**; they are kept because the history convention says the past explains the present.
Do not build from anything here. If a living doc references one of these by name, that reference
is historical context, not a dependency.

| File | Why it is here |
|---|---|
| `CLAUDE_CODE_PROMPTS.md` | The Stage 1–3 foundation build prompts. **Ran to completion** — the settlement loop is built and verified. |
| `CORE_POLISH_PROMPTS.md` | The Core Feel pass (camera/avatar, inspection panel, the wolf). **Ran to completion.** |
| `STAGE4_PROMPTS.md` | Stage 4 (factions, bounties, missions, threat). **Ran**, systems built but mostly unsurfaced — and the Stage 4–5 *design* was then superseded by `ROGUELITE_REWORK.md`, so these prompts have no successor pass. |
| `ART_PASS_PROMPT.md` | Asset hygiene + the content-height visual-scale pass. **Ran to completion** — `Anchoring` and the size constants are the living result. |
| `CLEANUP_PLAN.md` + `CLEANUP_PROMPTS.md` | The 2026-08-05 restructure (slim CLAUDE.md, docs/history split, Main.gd modules, asset triage). **Ran to completion** — the repo you are standing in is the output. Code comments still cite "CLEANUP_PLAN.md Pass 4"; that is this file. |
| `ROGUELITE_PROMPTS.md` | The R1 prompt set ("the world exists"). **R1 shipped to spec** (see the three R1 history files). Its closing wait-for-playtest rule lives on as the gate at the top of `prompts/R2_PROMPTS.md`. |
| `Warlords_World_Map_Scale_and_Exploration_Plan.docx` | The original map plan. Its `.md` conversion (`design/WORLD_MAP_PLAN.md`) is the live, tooling-readable spec and wins on any divergence; the `.docx` is the archived source. |

Filed 2026-08-09 as part of the documentation-hygiene pass (`docs/README.md` is the index of
what *is* live).
